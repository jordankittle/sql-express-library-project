extends ../layout 

block content

    h1 Update Book

    include errors

    form(action='/books/' + book.id, method='post')
        include form
    a(href='/books/' + book.id + '/delete')
        button(type='button') Delete Book