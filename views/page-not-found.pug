extends layout

block content
  h1= message
  p Sorry! We couldn't find the page you were looking for.
  br
  p
    a(class='button', href='all_books.html') Home
  h2= error.status
  pre #{error.stack}